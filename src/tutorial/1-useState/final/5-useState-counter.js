import React, { useState } from 'react';

const useStateCounter = () => {
    return (
        <React.Fragment>
            <h3>Use state counter example</h3>
        </React.Fragment>
    );
}

export default useStateCounter;